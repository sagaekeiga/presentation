<% if micropost.cliped_by? current_user %>
    <%= link_to "clipをやめる", micropost_clips_path(micropost.id), method: :delete, :id => "clips_ajax_#{micropost.id}", remote: true %>
<% else %>
    <%= link_to "clip", micropost_clips_path(micropost.id), method: :post, :id => "clips_ajax_#{micropost.id}", remote: true %>
<% end %>