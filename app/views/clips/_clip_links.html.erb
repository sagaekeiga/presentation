<% if micropost.cliped_by? current_user %>
    <%= link_to "CLIPをやめる", micropost_clips_path(micropost.id), method: :delete, :id => "clips_ajax_#{micropost.id}", :class => "sh_mi_lik", remote: true %>
<% else %>
    <%= link_to "CLIP", micropost_clips_path(micropost.id), method: :post, :id => "clips_ajax_#{micropost.id}", :class => "sh_mi_lik", remote: true %>
<% end %>